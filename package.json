{
  "name": "purescript-counter-web-app",
  "version": "0.1.0",
  "description": "PureScript counter web app using Flame",
  "keywords": [
    "PureScript",
    "counter",
    "web",
    "app",
    "Flame"
  ],
  "author": "Laurent Payot",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "spago test",
    "start": "spago build && vite",
    "build": "spago build && vite build",
    "serve": "cd dist && python3 -m http.server 5000",
    "do-update": "pnpm update --recursive --latest"
  },
  "devDependencies": {
    "esbuild": "^0.19.5",
    "purescript": "^0.15.12",
    "purs-tidy": "^0.10.0",
    "spago": "^0.21.0",
    "terser": "^5.22.0",
    "vite": "^4.5.0"
  }
}
