{
  "name": "purescript-app-example",
  "version": "0.1.0",
  "description": "PureScript example app Flame",
  "keywords": [
    "PureScript",
    "counter",
    "app",
    "Flame"
  ],
  "author": "Laurent Payot",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "spago test",
    "start": "spago build && vite",
    "build": "spago build && vite build",
    "serve": "cd dist && python3 -m http.server 5000"
  },
  "devDependencies": {
    "purescript": "^0.15.12",
    "purs-tidy": "^0.10.0",
    "spago": "^0.21.0",
    "terser": "^5.22.0",
    "vite": "^4.5.0"
  }
}
